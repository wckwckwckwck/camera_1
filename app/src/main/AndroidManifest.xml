<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.camera_1">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />


<!--    -->
<!--    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>-->
<!--    <uses-permission android:name="android.permission.INTERNET"/>-->

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HttpDemo"></activity>
        <activity android:name=".OKHttp_test"></activity>
        <!-- <activity android:name=".CameraActivity" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:screenOrientation="landscape"> -->
        <!-- &lt;!&ndash; configure this activity to use landscape orientation &ndash;&gt; -->
        <!--  -->
        <!-- </activity> -->
        <activity
            android:name=".tw_camera"
            android:label="@string/title_activity_tw_camera"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".startcamera" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>